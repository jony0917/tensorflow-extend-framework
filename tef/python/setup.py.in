import setuptools

setuptools.setup(
  name = "tef",
  version = '${PACKAGE_VERSION}',
  package_dir = {'':'${CMAKE_CURRENT_SOURCE_DIR}'},
  packages = setuptools.find_packages('${CMAKE_CURRENT_SOURCE_DIR}'),
  package_data = {'':['libtef_core.so']},
  install_requires = ['tensorflow'])

